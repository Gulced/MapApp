{
  "name": "mapapp-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "ng serve --proxy-config proxy.conf.json",
    "build": "ng build",
    "test": "ng test"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "ol": "^10.6.1"
  },
  "devDependencies": {
    "@angular/cli": "^16.2.0",
    "@angular/compiler-cli": "^16.2.0",
    "typescript": "^5.1.3"
  }
}
